---
title: ReadMe first
description: Your first document
---

## 我需要从哪里找到最新版本的 `solana` 客户端。

Github Repo : https://github.com/anza-xyz/agave

## 一键配置开发环境

```shell
curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
```

开发环境包含

1. `solana` CLI 环境
2. `anchor` 环境

## solana cli 基础使用
